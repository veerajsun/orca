import { MagicSiteConfig } from '../types/content';

/**
 * Magic Site External Links Configuration
 * Replace empty strings with external URLs when ready to enable redirection.
 */
export const magicSiteLinks: MagicSiteConfig = {
  movies: "",
  series: "",
  anime: "",
  kdrama: "",
  podcast: ""
};
